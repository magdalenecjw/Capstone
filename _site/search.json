[
  {
    "objectID": "Analysis/Setup/DataPrep.html",
    "href": "Analysis/Setup/DataPrep.html",
    "title": "Data Preparation",
    "section": "",
    "text": "Getting Started\nThe code chunk below uses p_load() function of pacman package to check if the required packages have been installed on the computer. If they are, the packages will be launched.\nThe packages used are:\n\narrow package to read and write Parquet files, which is the format that our source data is in.\nfs package for smoother file handling.\ntidyverse package for aspatial data wrangling.\nlubridate package to handle date and time data.\ndata.table package for fast aggregation of large datasets.\nsf package is used for importing, managing, and processing geospatial data.\nstplanr package is used to create and plot desire lines on maps.\ntmap package is used to visualize geospatial data on maps.\n\n\n\nCode\npacman::p_load(arrow, fs, tidyverse, lubridate, data.table, sf, stplanr, tmap)\n\n\n\n\nPreparing base map\nThe basemap is created using the URA Master Plan 2019, which includes sub-zone boundaries in Singapore. The code chunk below uses st_read() function of sf package to import MPSZ-2019 shapefile into R as a polygon feature data frame. As this set of geospatial data is in WGS-84 coordinate system, st_transform() function is used to convert it to the svy21 projected coordinate system (crs = 3414) that Singapore uses.\n\n\nCode\nmpsz = st_read(dsn = \"../data/geospatial\", \n               layer = \"MPSZ-2019\") %&gt;%\n  st_transform(crs = 3414)\n\n\nReading layer `MPSZ-2019' from data source \n  `C:\\magdalenecjw\\Capstone\\Analysis\\data\\geospatial' using driver `ESRI Shapefile'\nSimple feature collection with 332 features and 6 fields\nGeometry type: MULTIPOLYGON\nDimension:     XY\nBounding box:  xmin: 103.6057 ymin: 1.158699 xmax: 104.0885 ymax: 1.470775\nGeodetic CRS:  WGS 84\n\n\nqtm() function of tmap package is then used to plot a simple choropleth map to visualise the subzone boundaries.\n\n\nCode\ntmap_mode(\"plot\")\nqtm(mpsz)\n\n\n\n\n\nGiven that this geospatial dataset only has subzone boundaries without road network lines, there is a need to integrate the latter into the dataset to create a more comprehensive base map. The code chunk below uses st_read() function of sf package to import the OpenStreetMap &gt; Asia &gt; Malaysia, Singapore and Brunei shapefile extract from the GeoFabrik website. As this set of geospatial data is in WGS-84 coordinate system, st_transform() function is used to convert it to the svy21 projected coordinate system (crs = 3414) that Singapore uses.\n\n\nCode\nroads = st_read(dsn = \"../data/geospatial\", \n               layer = \"gis_osm_roads_free_1\") %&gt;%\n  st_transform(crs = 3414)\n\nmpsz_road &lt;- mpsz %&gt;%\n  st_intersection(roads)\n\n\ntm_shape() and tm_lines() functions of tmap package are then used to plot a simple choropleth map to visualise the roads within various planning subzones.\n\n\nCode\ntm_shape(mpsz_road %&gt;%\n           filter(PLN_AREA_N == \"ORCHARD\")) + \n  tm_lines()\n\n\n\n\n\n\n\nLoading Grab dataset\nThe dataset is provided by Grab over 10 separate files (in Apache Parquet file format), with each trajectory ID (primary identifier of each trip) found in multiple files. There is a need to join the 10 files before performing any further data wrangling. In the code chunk below, read_parquet() from arrow package is used to read the Parquet files. As there are 10 such files, a loop function is used to load each Parquet file within the file directory and stored into a list, which is finally then merged using bind_rows() function from dplyr package. Lastly, as the pingtimestamp field is in integer format, it is converted into date-time format using as_datetime() function of lubridate package\n\n\nCode\n# List Parquet files in the directory\nparquet_files &lt;- dir_ls(\"../data/\", regexp = \"\\\\.parquet$\")\n\n# Initialize an empty list to store dataframes\ndfs &lt;- list()\n\n# Loop through each Parquet file, read it, and store in the list\nfor (file_path in parquet_files) {\n  df &lt;- read_parquet(file_path)\n  dfs &lt;- append(dfs, list(df))\n}\n\n# Combine individual dataframes into one dataframe\ngrab &lt;- bind_rows(dfs) %&gt;%\n  # Convert pingtimestamp column to date-time format\n  mutate(pingtimestamp = as_datetime(pingtimestamp))\n\n# Remove objects that will no longer be used\nrm(df, dfs, file_path, parquet_files)\n\n\nFor sorting a large tibble data frame efficiently in R, while dplyr package offers the arrange() function, it typically does not perform the task as quickly as setorder() function of data.table package. data.table package often offers better performance for sorting large datasets due to its efficient memory management and optimized algorithms.\n\n\nCode\n# Set tibble dataframe as data.table \n# Then sort by trajectory ID and pingtimestamp\ngrab_sorted &lt;- setorder(as.data.table(grab), trj_id, pingtimestamp)\n\n# Convert back to tibble dataframe\ngrab_sorted &lt;- as_tibble(grab_sorted)\n\n# Remove objects that will no longer be used\nrm(grab)\n\n\n\n\nCode\ngrab_sf &lt;- grab_sorted %&gt;%\n  st_as_sf(coords = c(\"rawlng\", \"rawlat\"),\n                      crs = 4326) %&gt;%\n  st_transform(crs = 3414)\n\n\nAccuracy level is measured on the horizontal plane.\n\nOn Android devices, this is the radius within which the true location is likely to be found with a 68% probability. A value of 0 indicates no available data and is excluded from publishing.\nFor iOS devices, the reported coordinates mark the centre of the circle where the true location is randomly located, with the radius defined by the accuracy level. Negative values are invalid data and omitted.\n\nHuang et al. (2019) compared the accuracy levels by OS type to reveal significant differences, with iOS devices showing a wider range and less reliable location data, evidenced by a long tail in the distribution for accuracy levels. These findings highlight the need for careful consideration when using this data field. As such, the data is checked for the accuracy levels.\n\n\nCode\nmax &lt;- grab_sorted %&gt;%\n  filter(accuracy &gt;= 1000) %&gt;%\n  arrange(desc(accuracy))\n\nmax\n\n\n# A tibble: 186 × 9\n   trj_id drivi…¹ osname pingtimestamp       rawlat rawlng speed bearing accur…²\n   &lt;chr&gt;  &lt;chr&gt;   &lt;chr&gt;  &lt;dttm&gt;               &lt;dbl&gt;  &lt;dbl&gt; &lt;dbl&gt;   &lt;int&gt;   &lt;dbl&gt;\n 1 63624  car     ios    2019-04-21 23:30:08   1.32   104.    -1     252  149000\n 2 82964  car     ios    2019-04-11 05:07:50   1.28   104.    -1     335  149000\n 3 83631  car     ios    2019-04-19 06:50:12   1.36   104.    -1     327    5005\n 4 62994  car     ios    2019-04-20 06:33:55   1.27   104.    -1      54    2556\n 5 60322  car     ios    2019-04-18 18:47:37   1.27   104.    -1      59    2546\n 6 71273  car     ios    2019-04-19 08:04:49   1.27   104.    -1      72    2496\n 7 63242  car     ios    2019-04-11 08:09:31   1.27   104.    -1      52    2465\n 8 75289  car     ios    2019-04-17 12:34:42   1.28   104.    -1     265    2416\n 9 60547  car     ios    2019-04-10 00:34:46   1.27   104.    -1      48    2360\n10 51509  car     ios    2019-04-19 07:22:50   1.27   104.    -1     103    2358\n# … with 176 more rows, and abbreviated variable names ¹​driving_mode, ²​accuracy\n\n\nThere are two trj_id with a GPS ping of accuracy level of 149km recorded on an iOS device. Given that the main island of Singapore is about 49km from east to west and the radius of the circle encompassing the true location is defined by the accuracy level, this means the true location of these two GPS pings could be anywhere in Singapore. As such, these two GPS pings should be excluded given their high inaccuracy.\n\n\nCode\ngrab_sorted2 &lt;- grab_sorted %&gt;%\n  filter(!(accuracy == 149000))\n\n\n\n\nData Wrangling\nThere is a need to identify the Pick-up locations. In the code chunk below, group_by() function of dplyr package is used to group the data by trajectory ID, before taking the GPS coordinates (latitude and longitude) of the earliest GPS ping timestamp using filter() and min() function.\nSimilar steps are then performed to identify the Drop-off locations. group_by() function of dplyr package is used to group the data by trajectory ID, before taking the GPS coordinates (latitude and longitude) of the latest GPS ping timestamp using filter() and max() function.\n\n\nCode\n# Identify Pick-up locations\npickup_loc &lt;- grab_sorted2 %&gt;%\n  group_by(trj_id) %&gt;%\n  filter(pingtimestamp == min(pingtimestamp)) %&gt;%\n  ungroup() %&gt;%\n  select(trj_id, rawlng, rawlat, pingtimestamp) %&gt;%\n  rename(pickup_lng = rawlng, pickup_lat = rawlat)\n\n# Identify Drop-off locations\ndropoff_loc &lt;- grab_sorted2 %&gt;%\n  group_by(trj_id) %&gt;%\n  filter(pingtimestamp == max(pingtimestamp)) %&gt;%\n  ungroup() %&gt;%\n  select(trj_id, rawlng, rawlat, pingtimestamp) %&gt;%\n  rename(dropoff_lng = rawlng, dropoff_lat = rawlat)\n\n\n\n\nConverting aspatial data to geospatial data\nTo convert the aspatial data to geospatial data, the latitude (rawlat) and longitude (rawlng) columns will be used. These columns are in decimal degree format, indicating that the data is in wgs84 geographic coordinate system. The code chunk below converts the grab_sorted object into a sf data frame using st_as_sf() function of sf package then st_transform() function to convert it into the svy21 projected coordinate system that Singapore uses.\n\n\nCode\npickup_sf &lt;- st_as_sf(pickup_loc,\n                      coords = c(\"pickup_lng\", \"pickup_lat\"),\n                      crs = 4326) %&gt;%\n  st_transform(crs = 3414)\n\ndropoff_sf &lt;- st_as_sf(dropoff_loc,\n                       coords = c(\"dropoff_lng\", \"dropoff_lat\"),\n                       crs = 4326) %&gt;%\n  st_transform(crs = 3414)\n\n\nTo ensure the transformation has been done correctly, the newly created sf data frames will be visualised on a choropleth map using tmap package.\n\n\nCode\n# Visualise drop-off locations\ntm_shape(mpsz) + \n  tm_polygons() +\ntm_shape(pickup_sf) + \n  tm_dots()\n\n\n\n\n\n\n\nCode\n# Visualise drop-off locations\ntm_shape(mpsz) + \n  tm_polygons() +\ntm_shape(dropoff_sf) + \n  tm_dots()\n\n\n\n\n\n\n\nCreating O-D flow matrix\nApart from pick-up and drop-off point patterns, another objective of this study is to study the origin-destination flow patterns. As such, there is a need to create an origin-destination flow matrix where each row relates to one trajectory ID (trip) and represents travel from origin to destination.\nod_coords2line() function of stplanr package is used to convert origin-destination coordinates into desire lines. The function takes in a data frame or matrix representing the coordinates of origin-destination data, where the first two columns represent the coordinates of the origin points and the third and fourth columns represent the coordinates of the destination (in the same CRS).\n\n\nCode\nodflow &lt;- pickup_loc %&gt;%\n  left_join(dropoff_loc, by = \"trj_id\")\n\nodflow_sf &lt;- od_coords2line(odflow %&gt;%\n                              select(\"pickup_lng\",\"pickup_lat\",\"dropoff_lng\",\"dropoff_lat\"),\n                            crs = 4326) %&gt;%\n  st_transform(crs = 3414)\n\n\n\n\nCode\nodflow &lt;- odflow %&gt;%\n  mutate(duration = pingtimestamp.y - pingtimestamp.x)\n\nmean(odflow$duration)\n\n\nTime difference of 20.85986 mins\n\n\n\n\nCode\nodflow_sf &lt;- odflow_sf %&gt;%\n  mutate(distance = st_length(geometry))\n\nmean(odflow_sf$distance)\n\n\n14131.02 [m]"
  },
  {
    "objectID": "Analysis/KDE/KDE_spatstat.html#filter-by-planning-area",
    "href": "Analysis/KDE/KDE_spatstat.html#filter-by-planning-area",
    "title": "KDE Analysis using spatstat package",
    "section": "Filter by planning area",
    "text": "Filter by planning area\n\nareas &lt;- unique(mpsz$PLN_AREA_N)\n\nfor (i in areas){\n  mpsz_filter &lt;- mpsz %&gt;%\n  filter(PLN_AREA_N == i)\n  \n  filter_owin &lt;- as.owin(mpsz_filter)\n    \n  filter_ppp = pickup_ppp[filter_owin]\n  filter_ppp &lt;- rescale(filter_ppp, 1000, \"km\")\n    \n  # Initialize an error flag\n  error_occurred &lt;- FALSE\n  \n  # Attempt to calculate kde1\n  kde1 &lt;- tryCatch({\n    density(filter_ppp, sigma=bw.diggle, edge=TRUE, diggle=FALSE)\n  }, error = function(e) {\n    cat(\"Error in processing kde1 for area\", i, \": \", e$message, \"\\n\")\n    error_occurred &lt;&lt;- TRUE  # Update error flag\n    return(NULL)\n  })\n  \n  # Skip this iteration if an error occurred in kde1\n  if (error_occurred) {\n    next\n  }\n  \n  kde2 &lt;- density(filter_ppp, sigma=bw.diggle, edge=TRUE, diggle=TRUE)\n  \n  png(paste0(\"Pick-up - Edge Correction/pickup_edge_\",i,\".png\"), width=1000, height=500)\n  par(mfrow=c(1,2))\n  plot(kde1, main=\"Edge Correction - Default\")\n  plot(kde2, main=\"Edge Correction - Improved\")\n  dev.off()\n}"
  },
  {
    "objectID": "Analysis/KDE/KDE_spatstat.html#filter-by-planning-area-1",
    "href": "Analysis/KDE/KDE_spatstat.html#filter-by-planning-area-1",
    "title": "KDE Analysis using spatstat package",
    "section": "Filter by planning area",
    "text": "Filter by planning area\n\nareas &lt;- unique(mpsz$PLN_AREA_N)\n\n#areas &lt;- \"NOVENA\"\n\nfor (i in areas){\n  mpsz_filter &lt;- mpsz %&gt;%\n  filter(PLN_AREA_N == i)\n  \n  filter_owin &lt;- as.owin(mpsz_filter)\n    \n  filter_ppp = dropoff_ppp[filter_owin]\n  filter_ppp &lt;- rescale(filter_ppp, 1000, \"km\")\n    \n  # Initialize an error flag\n  error_occurred &lt;- FALSE\n  \n  # Attempt to calculate kde1\n  kde1 &lt;- tryCatch({\n    density(filter_ppp, sigma=bw.diggle, edge=TRUE, diggle=FALSE)\n  }, error = function(e) {\n    cat(\"Error in processing kde1 for area\", i, \": \", e$message, \"\\n\")\n    error_occurred &lt;&lt;- TRUE  # Update error flag\n    return(NULL)\n  })\n  \n  # Skip this iteration if an error occurred in kde1\n  if (error_occurred) {\n    next\n  }\n  \n  kde2 &lt;- density(filter_ppp, sigma=bw.diggle, edge=TRUE, diggle=TRUE)\n  \n  png(paste0(\"Drop-off - Edge Correction/dropoff_edge_\",i,\".png\"), width=1000, height=400)\n  par(mfrow=c(1,2))\n  plot(kde1, main=\"Edge Correction - Default\")\n  plot(kde2, main=\"Edge Correction - Improved\")\n  dev.off()\n}"
  },
  {
    "objectID": "Analysis/KDE/KDE_spatstat.html#filter-by-planning-area-2",
    "href": "Analysis/KDE/KDE_spatstat.html#filter-by-planning-area-2",
    "title": "KDE Analysis using spatstat package",
    "section": "Filter by planning area",
    "text": "Filter by planning area\n\nareas &lt;- unique(mpsz$REGION_N)\n\nfor (i in areas){\n  mpsz_filter &lt;- mpsz %&gt;%\n  filter(REGION_N == i)\n  \n  filter_owin &lt;- as.owin(mpsz_filter)\n    \n  filter_ppp = pickup_ppp[filter_owin]\n  filter_ppp &lt;- rescale(filter_ppp, 1000, \"km\")\n    \n  # Initialize an error flag\n  error_occurred &lt;- FALSE\n  \n  # Attempt to calculate kde1\n  kde1 &lt;- tryCatch({\n    density(filter_ppp, sigma=bw.diggle, edge=TRUE)\n  }, error = function(e) {\n    cat(\"Error in processing kde1 for area\", i, \": \", e$message, \"\\n\")\n    error_occurred &lt;&lt;- TRUE  # Update error flag\n    return(NULL)\n  })\n  \n  # Skip this iteration if an error occurred in kde1\n  if (error_occurred) {\n    next\n  }\n  \n  kde2 &lt;- density(filter_ppp, sigma=bw.ppl, edge=TRUE)\n  kde3 &lt;- densityAdaptiveKernel(filter_ppp, edge=TRUE)\n  \n  png(paste0(\"Pick-up - Bandwidth/pickup_bw_\",i,\".png\"), width=800, height=700)\n  par(mfrow=c(2,2))\n  plot(kde1, main=\"Fixed - bw.diggle\")\n  plot(kde2, main=\"Fixed - bw.ppl\")\n  plot(kde3, main=\"Adaptive - bw.abram\")\n  dev.off()\n}"
  },
  {
    "objectID": "Analysis/data/geospatial/MPSZ-2019.html",
    "href": "Analysis/data/geospatial/MPSZ-2019.html",
    "title": "Capstone Journey",
    "section": "",
    "text": "&lt;!DOCTYPE qgis PUBLIC ‘http://mrcc.com/qgis.dtd’ ‘SYSTEM’&gt;     dataset\n\n\n        0 0     false"
  },
  {
    "objectID": "about.html",
    "href": "about.html",
    "title": "About",
    "section": "",
    "text": "About this site\n\n1 + 1\n\n[1] 2"
  },
  {
    "objectID": "Analysis/KDE/TKDE_ks.html",
    "href": "Analysis/KDE/TKDE_ks.html",
    "title": "TKDE Analysis using ks package",
    "section": "",
    "text": "Getting Started\nThe code chunk below uses p_load() function of pacman package to check if the required packages have been installed on the computer. If they are, the packages will be launched.\nThe packages used are:\n\nsf package is used for importing, managing, and processing geospatial data.\ntidyverse package for aspatial data wrangling.\nks package to perform multivariate kernel density estimation (KDE).\n\n\npacman::p_load(sf, tidyverse, ks, lubridate, rgl)\n\nIn the code chunk below, the cleaned data files are loaded into R.\n\n# Load base map\nmpsz = st_read(dsn = \"../data/geospatial\", \n               layer = \"MPSZ-2019\") %&gt;%\n  st_transform(crs = 3414) %&gt;%\n  summarize()\n\n# Load rds file\npickup_loc &lt;- read_rds(\"../data/rds/pickup_loc.rds\") %&gt;%\n  mutate(pingtimestamp = as.integer(pingtimestamp)) %&gt;%\n  select(-trj_id)\ndropoff_loc &lt;- read_rds(\"../data/rds/dropoff_loc.rds\") %&gt;%\n  mutate(pingtimestamp = as.integer(pingtimestamp)) %&gt;%\n  select(-trj_id)\npickup_sf &lt;- st_as_sf(read_rds(\"../data/rds/pickup_sf.rds\"))\ndropoff_sf &lt;- st_as_sf(read_rds(\"../data/rds/dropoff_sf.rds\"))\n\n\n\nWithout normalization\n\nkde_ks_original &lt;- kde(pickup_loc, H = Hlscv(pickup_loc))\n\nsummary(kde_ks_original$estimate)\n\n\n\nWith normalization\n\n# Normalize the data to the range [0, 1]\nnormalize &lt;- function(x) {\n  return ((x - min(x)) / (max(x) - min(x)))\n}\n\npickup_norm &lt;- as.data.frame(lapply(pickup_loc, normalize))\n\n# Store the original ranges for scaling back\nrange_x &lt;- range(pickup_loc$pickup_lng)\nrange_y &lt;- range(pickup_loc$pickup_lng)\nrange_time &lt;- range(pickup_loc$pingtimestamp)\n\nkde_ks &lt;- kde(pickup_norm, H = Hlscv(pickup_norm))\n\nplot(kde_ks)"
  }
]